# private-state CLI

The CLI documentation has been merged into the parent DApp README.

Use [../README.md](../README.md) as the single source of truth for:

- CLI command order and command descriptions
- getter output formats
- wallet and workspace layout
- local anvil and end-to-end workflows

The CLI entrypoint remains:

```bash
node packages/apps/private-state/cli/private-state-bridge-cli.mjs <command> ...
```
