interface:
  display_name: "Deployment Source Integrity"
  short_description: "Use latest registry dependencies and require source-safe deployments."
  default_prompt: "Use this skill before bridge or DApp deployment, DApp registration, or deployment metadata upload so external npm dependencies are upgraded to registry latest, sourceUrl metadata points to a commit on remote main, and no-op contract deployments are rejected unless forced."
