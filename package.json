{
  "type": "module",
  "scripts": {
    "gen:tokamak-vk": "node script/generate-tokamak-verifier-key.js",
    "build": "npm run gen:tokamak-vk && forge build",
    "test": "npm run gen:tokamak-vk && forge test --offline"
  },
  "dependencies": {
    "ethers": "^6.14.1",
    "fs": "^0.0.1-security",
    "js-sha3": "^0.9.3",
    "msgpackr": "^1.11.3"
  }
}
