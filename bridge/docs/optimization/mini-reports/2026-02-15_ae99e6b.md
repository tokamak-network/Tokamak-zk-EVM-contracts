# Mini Report: 2026-02-15 (ae99e6b)

## Gas usage
- 655,104

## Commits
- `65ad49e` Update reporting notes for latest verifier gas snapshot
- `a33c220` Refresh reporting series with latest folded gas snapshot
- `1e4c03e` Apply repository-wide pending updates
- `a5273e9` Update LHS+AUX summary table for revised pairing equation
- `e476a81` Sync verifier spec updates and refactor computeAPUB domain params
- `8c1455f` Generate Tokamak VK from sigma_verify at build time
- `ae99e6b` Commit all current workspace changes

## Change Analysis
1. **Proposition:** `computeAPUB` now evaluates the free-input polynomial on a 64-point domain (`l_free = 64`) using `OMEGA_64`, reducing loop work in the main runtime hotspot.  
Proof (excerpt):
```text
function computeAPUB() {
    let l_free := 64
    let omega := OMEGA_64
    let chi_n := modexp(chi, l_free)
}
```

2. **Proposition:** The current pairing path includes one explicit G1ADD before the final pairing, so the precompile mix is `modexp x7`, `G1ADD x1`, `G1MSM x3`, `pairing x1`.  
Proof (excerpt):
```text
[375] 0x000...000b::00000000(...) [staticcall]
[363700] 0x000...000F::00000000(...) [staticcall]
```

3. **Proposition:** Verification key sourcing moved to build-time generation from `sigma_verify.json`, while `_loadVerificationKey` still loads constants into verifier memory at runtime.  
Proof (excerpt):
```text
package.json: "gen:tokamak-vk": "node scripts/generate-tokamak-verifier-key.js"
TokamakVerifier.sol: import TokamakVerifierKeyGenerated
_loadVerificationKey(): lagrangeKlXPart1 = TokamakVerifierKeyGenerated.LAGRANGE_KL_X_PART1
```

4. **Proposition:** This snapshot forms a new Source Series row because the decrease vs the previous snapshot exceeds the 5% threshold.  
Proof (excerpt):
```text
previous = 785,531 (f483cc4)
current  = 655,104 (ae99e6b)
reduction = 130,427 / 785,531 = 16.61%
```
